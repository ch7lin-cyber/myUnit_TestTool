# Mocks

Place replacements for hardware, clock, NVM, communication and other external
dependencies here. A mock must record calls and allow each test to inject
return values or failures deterministically.
