# Test results

`tools/run_tests.py` writes the latest machine-readable result to
`TestResult/latest.json`. Formal projects should also retain JUnit XML,
coverage HTML/XML and compiler/sanitizer logs. Commit only reviewed evidence
required by the project's traceability policy.
